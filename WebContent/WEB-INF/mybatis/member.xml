<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="adminMember">

	<select id="selectAdminMember" parameterType="int" resultType="com.fireBusters.web.dto.AdminMember">
 		 select fire_station_id, fire_station_password from fire_station where fire_station_id=#{fire_station_id}
	</select>

	<select id="selectReport" parameterType="int" resultType="com.fireBusters.web.dto.AdminBoard">
			select to_char(report_date, 'MM/DD HH24:MI:SS') report_date, report_lat, report_lon, fire_station_name
				from report r, fire_station f
				where r.fire_station_no = f.fire_station_id and fire_station_no=#{id}
	</select>
	
	
	
</mapper>